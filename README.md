# PlayRacket — artes publicadas

Hospedagem pública das artes aprovadas do @playracketbr (exigência da API do
Instagram: imagem via URL pública). Gerado automaticamente pelo
`publicar-insta.js` do repo playracket-social.
